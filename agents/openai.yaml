interface:
  display_name: "Claude Review"
  short_description: "Native Claude Opus 5 review gate"
  default_prompt: "Use $claude-review to run one visible Claude Opus 5 review, wait locally for its compact handoff, verify findings proportionally, and checkpoint before editing."

policy:
  allow_implicit_invocation: false
